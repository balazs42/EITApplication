<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ElectricalImpedanceTomography.Views.MeshingPage"
             xmlns:skia="clr-namespace:SkiaSharp.Views.Maui.Controls;assembly=SkiaSharp.Views.Maui.Controls"
             xmlns:viewmodels="clr-namespace:ElectricalImpedanceTomography.ViewModels"
             xmlns:controls="clr-namespace:ElectricalImpedanceTomography.Controls"
             Title="MeshingPage"
             x:DataType="viewmodels:MeshingPageViewModel">

    <Grid RowDefinitions="Auto, Auto, Auto, Auto">
        <controls:NavBarControl Grid.Row="0" Grid.ColumnSpan="1" CurrentPageName="MeshingPage"/>

        <!-- Parameter selection -->
        <Grid Grid.Row="1" ColumnDefinitions="Auto,Auto,Auto,Auto,Auto,Auto" RowDefinitions="Auto,Auto,Auto,Auto,Auto">
            <Label Grid.Column="0" Text="Mesh" VerticalOptions="Center"/>
            <Picker Grid.Column="1" ItemsSource="{Binding MeshTypes}" SelectedItem="{Binding SelectedMeshType}" WidthRequest="100"/>

            <!-- FEM parameters -->
            <Label Grid.Row="1" Grid.Column="0" Text="Layers" IsVisible="{Binding IsFEM}"/>
            <Entry Grid.Row="1" Grid.Column="1" Text="{Binding Layers}" WidthRequest="60" IsVisible="{Binding IsFEM}" Keyboard="Numeric"/>
            <Label Grid.Row="1" Grid.Column="2" Text="Boundary Vertices" IsVisible="{Binding IsFEM}"/>
            <Entry Grid.Row="1" Grid.Column="3" Text="{Binding BoundaryFEMVertexCount}" WidthRequest="60" IsVisible="{Binding IsFEM}" Keyboard="Numeric"/>

            <!-- LBM parameters -->
            <Label Grid.Row="1" Grid.Column="0" Text="Nx" IsVisible="{Binding IsLBM}"/>
            <Entry Grid.Row="1" Grid.Column="1" Text="{Binding Nx}" WidthRequest="60" IsVisible="{Binding IsLBM}" Keyboard="Numeric"/>
            <Label Grid.Row="1" Grid.Column="2" Text="Ny" IsVisible="{Binding IsLBM}"/>
            <Entry Grid.Row="1" Grid.Column="3" Text="{Binding Ny}" WidthRequest="60" IsVisible="{Binding IsLBM}" Keyboard="Numeric"/>

            <!-- Common parameters -->
            <Label Grid.Row="2" Grid.Column="0" Text="Electrodes"/>
            <Entry Grid.Row="2" Grid.Column="1" Text="{Binding ElectrodeCount}" WidthRequest="60" Keyboard="Numeric"/>

            <Label Grid.Row="2" Grid.Column="2" Text="Geometry"/>
            <Picker Grid.Row="2" Grid.Column="3" ItemsSource="{Binding GeometryTypes}" SelectedItem="{Binding SelectedGeometry}" WidthRequest="120"/>

            <Label Grid.Row="3" Grid.Column="0" Text="Custom" IsVisible="{Binding IsCustomGeometry}"/>
            <Entry Grid.Row="3" Grid.Column="1" Text="{Binding CustomPerimeter}" WidthRequest="200" IsVisible="{Binding IsCustomGeometry}"/>

            <CheckBox Grid.Row="3" Grid.Column="2" IsChecked="{Binding InhomogenityEditing}"/>
            <Entry Grid.Row="3" Grid.Column="3" Text="{Binding InhomogenityValue}" WidthRequest="80" IsVisible="{Binding InhomogenityEditing}" Keyboard="Numeric"/>

            <Label Grid.Row="4" Grid.Column="0" Text="Contact Imp."/>
            <Entry Grid.Row="4" Grid.Column="1" Text="{Binding ElectrodeContactImpedance}" WidthRequest="60" Keyboard="Numeric"/>
            <Label Grid.Row="4" Grid.Column="2" Text="Electrode Size" IsVisible="{Binding IsFEM}"/>
            <Entry Grid.Row="4" Grid.Column="3" Text="{Binding ElectrodeSize}" WidthRequest="60" IsVisible="{Binding IsFEM}" Keyboard="Numeric"/>
        </Grid>

        <Border Grid.Row="2"
                Stroke="DarkGray"
                StrokeThickness="2"
                Margin="10"
                HorizontalOptions="Center"
                VerticalOptions="Center">
            <skia:SKCanvasView x:Name="MeshCanvas"
                               PaintSurface="OnMeshCanvasPaintSurface"
                               EnableTouchEvents="True"
                               Touch="OnMeshCanvasTouch"
                               HeightRequest="500"
                               WidthRequest="500"
                               Margin="10" />
        </Border>

        <Grid Grid.Row="3" ColumnDefinitions="Auto,Auto,Auto" ColumnSpacing="10" HorizontalOptions="Center">
            <Button Grid.Column="0" Text="Save" Clicked="OnSaveClicked"/>
            <Button Grid.Column="1" Text="Load" Clicked="OnLoadClicked"/>
            <Button Grid.Column="2" Text="Generate" Clicked="OnGenerateClicked"/>
        </Grid>
    </Grid>
</ContentPage>