<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ElectricalImpedanceTomography.Views.MeshingPage"
             xmlns:skia="clr-namespace:SkiaSharp.Views.Maui.Controls;assembly=SkiaSharp.Views.Maui.Controls"
             xmlns:viewmodels="clr-namespace:ElectricalImpedanceTomography.ViewModels"
             xmlns:controls="clr-namespace:ElectricalImpedanceTomography.Controls"
             Title="MeshingPage"
             x:DataType="viewmodels:MeshingPageViewModel">

    <Grid RowDefinitions="Auto, Auto, Auto, Auto">
        <controls:NavBarControl Grid.Row="0" Grid.ColumnSpan="1" CurrentPageName="MeshingPage"/>

        <!-- Parameter selection -->
        <ScrollView Grid.Row="1" Orientation="Horizontal">
            <HorizontalStackLayout Spacing="10" Padding="5">
                <Label Text="Mesh" VerticalOptions="Center"/>
                <Picker ItemsSource="{Binding MeshTypes}" SelectedItem="{Binding SelectedMeshType}" WidthRequest="100"/>

                <!-- FEM parameters -->
                <Label Text="Layers" IsVisible="{Binding IsFEM}" VerticalOptions="Center"/>
                <Entry Text="{Binding Layers}" WidthRequest="60" IsVisible="{Binding IsFEM}" Keyboard="Numeric"/>
                <Label Text="Boundary Vertices" IsVisible="{Binding IsFEM}" VerticalOptions="Center"/>
                <Entry Text="{Binding BoundaryFEMVertexCount}" WidthRequest="60" IsVisible="{Binding IsFEM}" Keyboard="Numeric"/>

                <!-- LBM parameters -->
                <Label Text="Nx" IsVisible="{Binding IsLBM}" VerticalOptions="Center"/>
                <Entry Text="{Binding Nx}" WidthRequest="60" IsVisible="{Binding IsLBM}" Keyboard="Numeric"/>
                <Label Text="Ny" IsVisible="{Binding IsLBM}" VerticalOptions="Center"/>
                <Entry Text="{Binding Ny}" WidthRequest="60" IsVisible="{Binding IsLBM}" Keyboard="Numeric"/>

                <!-- Common parameters -->
                <Label Text="Electrodes" VerticalOptions="Center"/>
                <Entry Text="{Binding ElectrodeCount}" WidthRequest="60" Keyboard="Numeric"/>

                <Label Text="Geometry" VerticalOptions="Center"/>
                <Picker ItemsSource="{Binding GeometryTypes}" SelectedItem="{Binding SelectedGeometry}" WidthRequest="120"/>

                <Label Text="Custom" IsVisible="{Binding IsCustomGeometry}" VerticalOptions="Center"/>
                <Entry Text="{Binding CustomPerimeter}" WidthRequest="200" IsVisible="{Binding IsCustomGeometry}"/>

                <CheckBox IsChecked="{Binding InhomogenityEditing}"/>
                <Label Text="EditConductivities" VerticalOptions="Center"/>
                <Entry Text="{Binding InhomogenityValue}" WidthRequest="80" IsVisible="{Binding InhomogenityEditing}" Keyboard="Numeric"/>

                <Label Text="Contact Imp." VerticalOptions="Center"/>
                <Entry Text="{Binding ElectrodeContactImpedance}" WidthRequest="60" Keyboard="Numeric"/>
                <Label Text="Electrode Size" IsVisible="{Binding IsFEM}" VerticalOptions="Center"/>
                <Entry Text="{Binding ElectrodeSize}" WidthRequest="60" IsVisible="{Binding IsFEM}" Keyboard="Numeric"/>
            </HorizontalStackLayout>
        </ScrollView>

        <Border Grid.Row="2"
                Stroke="DarkGray"
                StrokeThickness="2"
                Margin="10"
                HorizontalOptions="Center"
                VerticalOptions="Center">
            <skia:SKCanvasView x:Name="MeshCanvas"
                               PaintSurface="OnMeshCanvasPaintSurface"
                               EnableTouchEvents="True"
                               Touch="OnMeshCanvasTouch"
                               HeightRequest="500"
                               WidthRequest="500"
                               Margin="10" />
        </Border>

        <Grid Grid.Row="3" ColumnDefinitions="Auto,Auto,Auto" ColumnSpacing="10" HorizontalOptions="Center">
            <Button Grid.Column="0" Text="Save" Clicked="OnSaveClicked"/>
            <Button Grid.Column="1" Text="Load" Clicked="OnLoadClicked"/>
            <Button Grid.Column="2" Text="Generate" Clicked="OnGenerateClicked"/>
        </Grid>
    </Grid>
</ContentPage>