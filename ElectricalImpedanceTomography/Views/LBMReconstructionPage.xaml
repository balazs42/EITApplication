<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ElectricalImpedanceTomography.Views.LBMReconstructionPage"
             xmlns:viewmodels="clr-namespace:ElectricalImpedanceTomography.ViewModels"
             xmlns:skia="clr-namespace:SkiaSharp.Views.Maui.Controls;assembly=SkiaSharp.Views.Maui.Controls"
             xmlns:controls="clr-namespace:ElectricalImpedanceTomography.Controls"
             xmlns:converters="clr-namespace:ElectricalImpedanceTomography.Converters"
             x:DataType="viewmodels:LBMReconstructionPageViewModel"          
             Title="ReconstructionPage">
    <ContentPage.Resources>
        <Style TargetType="Picker">
            <Setter Property="FontAttributes" Value="Bold"/>
        </Style>
        <converters:EnumDescriptionConverter x:Key="EnumDesc" />
    </ContentPage.Resources>
    <Grid RowDefinitions="Auto, Auto, Auto">
        <Grid RowDefinitions="Auto, Auto, *" ColumnDefinitions="*, Auto">
            <controls:NavBarControl Grid.Row="0" Grid.ColumnSpan="2" CurrentPageName="LBMReconstructionPage"/>
            <Grid Grid.Row="1"
                  Grid.Column="0"
                  ColumnDefinitions="Auto, Auto, Auto, Auto, Auto, Auto, Auto"
                  Padding="1"
                  ColumnSpacing="20"
                  HorizontalOptions="Center">
                <Picker x:Name="DESolverPicker"
                        Grid.Column="0"
                        Title="Differential Eq. Solver"
                        ItemsSource="{Binding DifferentialEquationSolverOptions}"
                        SelectedItem="{Binding ReconstructionParameters.DifferentialEquationSolver}"
                        ItemDisplayBinding="{Binding ., Converter={StaticResource EnumDesc}}"/>
                <Picker x:Name="RegulariaztionPicker"
                        Grid.Column="1"
                        Title="Regularisation"
                        ItemsSource="{Binding RegularizationTechniqueOptions}"
                        SelectedItem="{Binding ReconstructionParameters.RegularizationTechnique}"
                        ItemDisplayBinding="{Binding ., Converter={StaticResource EnumDesc}}" />
                <Picker x:Name="ErrorMetricPicker"
                        Grid.Column="2"
                        Title="Error metric"
                        ItemsSource="{Binding ErrorMetricOptions}"
                        SelectedItem="{Binding ReconstructionParameters.ErrorMetric}"
                        ItemDisplayBinding="{Binding ., Converter={StaticResource EnumDesc}}" />
                <Picker x:Name="NumericSolverPicker"
                        Grid.Column="3"
                        Title="Numeric solver"
                        ItemsSource="{Binding NumericSolverOptions}"
                        SelectedItem="{Binding ReconstructionParameters.NumericSolver}"
                        ItemDisplayBinding="{Binding ., Converter={StaticResource EnumDesc}}" />
                <Picker x:Name="NumericOptimizerPicker"
                        Grid.Column="4"
                        Title="Optimizer"
                        ItemsSource="{Binding NumericOptimizerOptions}"
                        SelectedItem="{Binding ReconstructionParameters.NumericOptimizer}"
                        ItemDisplayBinding="{Binding ., Converter={StaticResource EnumDesc}}" />
                <Picker x:Name="PotentialModePicker"
                        Grid.Column="5"
                        Title="Potential Display Mode"
                        SelectedIndexChanged="OnPotentialModeChanged">
                    <Picker.Items>
                        <x:String>Default</x:String>
                        <x:String>Grayscale</x:String>
                        <x:String>Inverted</x:String>
                        <x:String>Heatmap</x:String>
                        <x:String>Rainbow</x:String>
                    </Picker.Items>
                </Picker>
                <Button Grid.Column="6"
                        BackgroundColor="Gray"
                        TextColor="White"
                        FontSize="Small"
                        Padding="10"
                        Margin="10"
                        Text="Start Reconstruction"
                        Clicked="OnStartReconstruction"/>
            </Grid>
            <Grid Grid.Row="2"
                  RowDefinitions="Auto, *"
                  ColumnDefinitions="Auto, Auto, Auto"
                  ColumnSpacing="10"
                  Padding="10">
                
                <Label Grid.Row="0"
                       Grid.Column="0" 
                       FontAttributes="Bold"
                       HorizontalOptions="Center"
                       Text="LBM Grid:"/>
                <Border Grid.Row="1"
                        Grid.Column="0"
                        Stroke="DarkGray" 
                        StrokeThickness="2"
                        Margin="10"
                        HorizontalOptions="Center" 
                        VerticalOptions="Center">
                    
                    <skia:SKCanvasView x:Name="canvasView"
                                       WidthRequest="500" 
                                       HeightRequest="500"
                                       PaintSurface="OnCanvasViewPaintSurface"
                                       EnableTouchEvents="True"
                                       Touch="OnCanvasTouch">
                    </skia:SKCanvasView>
                </Border>

                <Label Text="Potential Field" 
                       Grid.Row="0" 
                       Grid.Column="1"
                       FontAttributes="Bold" 
                       HorizontalOptions="Center" />
                
                <Border Grid.Row="1"
                        Grid.Column="1"
                        Stroke="DarkGray" 
                        StrokeThickness="2"
                        Margin="10"
                        HorizontalOptions="Center" VerticalOptions="Center">
                    <skia:SKCanvasView x:Name="PotentialResultCanvas"
                                       PaintSurface="OnPotentialResultPaintSurface"
                                       EnableTouchEvents="False"
                                       HeightRequest="500"
                                       WidthRequest="500"
                                       Margin="10">
                    </skia:SKCanvasView>
                </Border>

                <Label Text="Current Amplitude Field" 
                       Grid.Row="0" 
                       Grid.Column="2"
                       FontAttributes="Bold" 
                       HorizontalOptions="Center" />

                <Border Grid.Row="1"
                        Grid.Column="2"
                        Stroke="DarkGray" 
                        StrokeThickness="2"
                        Margin="10"
                        HorizontalOptions="Center" 
                        VerticalOptions="Center">
                    <skia:SKCanvasView x:Name="CurrentAmplitudeCanvas"
                                       PaintSurface="OnPaintCurrentAmplitudeSurface"
                                       EnableTouchEvents="False"
                                       HeightRequest="500"
                                       WidthRequest="500"
                                       Margin="10"> 
                    </skia:SKCanvasView>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</ContentPage>