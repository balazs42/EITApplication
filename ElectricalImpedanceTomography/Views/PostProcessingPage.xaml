<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:ElectricalImpedanceTomography.ViewModels"
             xmlns:post="clr-namespace:Utility.Classes.PostProcessing;assembly=Utility"
             x:Class="ElectricalImpedanceTomography.Views.PostProcessingPage"
             Title="Post Processing"
             x:DataType="viewmodels:PostProcessingPageViewModel"
             Shell.NavBarIsVisible="False"
             BackgroundColor="#090b10">

    <ContentPage.Resources>
        <ResourceDictionary>
            <Color x:Key="AppBackground">#090b10</Color>
            <Color x:Key="PanelBackground">#0E1116</Color>
            <Color x:Key="InputBackground">#050608</Color>
            <Color x:Key="BorderColor">#1AFFFFFF</Color>
            <Color x:Key="PrimaryAccent">#06b6d4</Color>
            <Color x:Key="TextPrimary">#e2e8f0</Color>
            <Color x:Key="TextSecondary">#94a3b8</Color>
            <Color x:Key="TextMuted">#64748b</Color>

            <Style TargetType="Label" x:Key="HeaderLabel">
                <Setter Property="FontSize" Value="Small"/>
                <Setter Property="FontAttributes" Value="Bold"/>
                <Setter Property="TextColor" Value="{StaticResource TextSecondary}"/>
                <Setter Property="TextTransform" Value="Uppercase"/>
                <Setter Property="Margin" Value="0,16,0,8"/>
            </Style>

            <Style TargetType="Label" x:Key="PropLabel">
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="TextColor" Value="{StaticResource TextSecondary}"/>
                <Setter Property="VerticalOptions" Value="Center"/>
            </Style>

            <Style TargetType="Button" x:Key="ToolbarBtn">
                <Setter Property="BackgroundColor" Value="#1A1D24"/>
                <Setter Property="TextColor" Value="{StaticResource TextPrimary}"/>
                <Setter Property="FontSize" Value="Small"/>
                <Setter Property="FontAttributes" Value="Italic"/>
                <Setter Property="BorderColor" Value="{StaticResource BorderColor}"/>
                <Setter Property="BorderWidth" Value="1"/>
                <Setter Property="CornerRadius" Value="4"/>
                <Setter Property="HeightRequest" Value="32"/>
                <Setter Property="Padding" Value="12,0"/>
            </Style>

            <Style TargetType="Button" x:Key="PrimaryBtn">
                <Setter Property="BackgroundColor" Value="#0891b2"/>
                <Setter Property="TextColor" Value="White"/>
                <Setter Property="FontSize" Value="Small"/>
                <Setter Property="FontAttributes" Value="Bold"/>
                <Setter Property="BorderColor" Value="#06b6d4"/>
                <Setter Property="BorderWidth" Value="1"/>
                <Setter Property="CornerRadius" Value="4"/>
                <Setter Property="HeightRequest" Value="32"/>
                <Setter Property="Padding" Value="12,0"/>
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid>
        <!-- Main Content Area -->
        <Grid Grid.Row="1" ColumnDefinitions="340, *, 280">

            <!-- Left sidebar mirrors meshing/reconstruction styling -->
            <Border Grid.Column="0" BackgroundColor="{StaticResource PanelBackground}" Stroke="{StaticResource BorderColor}" StrokeThickness="0">
                <Grid RowDefinitions="Auto, *" Padding="16">
                    <VerticalStackLayout Grid.Row="0" Spacing="8">
                        <Label Text="Post Processing" FontAttributes="Bold" FontSize="18" TextColor="{StaticResource TextPrimary}"/>
                        <Label Text="{Binding ActiveSource, StringFormat='Active dataset: {0}'}" TextColor="{StaticResource TextSecondary}" FontSize="12"/>
                        <Label Text="{Binding LastSavedPath, StringFormat='Saved: {0}'}" TextColor="{StaticResource TextMuted}" FontSize="11" LineBreakMode="WordWrap"/>
                        <HorizontalStackLayout Spacing="8">
                            <Button Text="Load" Command="{Binding LoadLatestWorkspaceResultCommand}" Style="{StaticResource PrimaryBtn}" HeightRequest="32"/>
                            <Button Text="Import" Command="{Binding ImportSavedResultAsyncCommand}" Style="{StaticResource ToolbarBtn}" HeightRequest="32"/>
                        </HorizontalStackLayout>
                    </VerticalStackLayout>

                    <ScrollView Grid.Row="1" Padding="0,10,0,0">
                        <VerticalStackLayout Spacing="16">
                            <Border BackgroundColor="#0b0e12" Stroke="{StaticResource BorderColor}" StrokeShape="RoundRectangle 8" Padding="12">
                                <VerticalStackLayout Spacing="10">
                                    <Label Text="Post Processing Options" Style="{StaticResource HeaderLabel}" Margin="0,0,0,6"/>
                                    <CollectionView ItemsSource="{Binding PostProcessingOptions}" SelectionMode="Single" SelectedItem="{Binding SelectedPostProcessor}" HeightRequest="260">
                                        <CollectionView.ItemTemplate>
                                            <DataTemplate x:DataType="post:IPostProcessing">
                                                <Border BackgroundColor="#13161C" Stroke="{StaticResource BorderColor}" StrokeShape="RoundRectangle 6" Padding="10">
                                                    <VerticalStackLayout>
                                                        <Label Text="{Binding Name}" TextColor="{StaticResource TextPrimary}" FontAttributes="Bold"/>
                                                        <Label Text="{Binding Description}" TextColor="{StaticResource TextMuted}" FontSize="12" LineBreakMode="WordWrap"/>
                                                    </VerticalStackLayout>
                                                </Border>
                                            </DataTemplate>
                                        </CollectionView.ItemTemplate>
                                    </CollectionView>
                                    <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
                                        <Button Text="Apply" Command="{Binding ApplySelectedPostProcessingCommand}" Style="{StaticResource PrimaryBtn}"/>
                                        <Button Grid.Column="1" Text="Clear" Command="{Binding ClearHistoryCommand}" Style="{StaticResource ToolbarBtn}"/>
                                    </Grid>
                                    <Button Text="Save Processed" Command="{Binding SaveProcessedAsyncCommand}" BackgroundColor="#1A06b6d4" TextColor="{StaticResource PrimaryAccent}" BorderColor="#3306b6d4" BorderWidth="1" FontAttributes="Bold" HeightRequest="40" CornerRadius="6" FontSize="11"/>
                                </VerticalStackLayout>
                            </Border>

                            <Border BackgroundColor="#0b0e12" Stroke="{StaticResource BorderColor}" StrokeShape="RoundRectangle 8" Padding="12">
                                <VerticalStackLayout Spacing="8">
                                    <Label Text="History" Style="{StaticResource HeaderLabel}" Margin="0,0,0,6"/>
                                    <CollectionView ItemsSource="{Binding HistoryLog}" HeightRequest="120">
                                        <CollectionView.ItemTemplate>
                                            <DataTemplate>
                                                <Label Text="{Binding .}" TextColor="{StaticResource TextSecondary}" FontSize="12" LineBreakMode="WordWrap"/>
                                            </DataTemplate>
                                        </CollectionView.ItemTemplate>
                                    </CollectionView>
                                    <Button Text="Export" Command="{Binding ExportDataCommand}" Style="{StaticResource ToolbarBtn}"/>
                                </VerticalStackLayout>
                            </Border>
                        </VerticalStackLayout>
                    </ScrollView>
                </Grid>
            </Border>

            <!-- Center content uses the same canvas shell as meshing/reconstruction pages without custom rendering logic -->
            <Grid Grid.Column="1" RowDefinitions="56, *" BackgroundColor="{StaticResource AppBackground}">
                <Border Grid.Row="0" BackgroundColor="{StaticResource PanelBackground}" Stroke="{StaticResource BorderColor}" StrokeThickness="0">
                    <Grid ColumnDefinitions="Auto, *, Auto" Padding="16,0">
                        <HorizontalStackLayout Grid.Column="0" Spacing="8" VerticalOptions="Center">
                            <Button Style="{StaticResource PrimaryBtn}" Text="Apply" Command="{Binding ApplySelectedPostProcessingCommand}" />
                            <BoxView WidthRequest="1" Color="{StaticResource BorderColor}" Margin="4,8"/>
                            <Button Style="{StaticResource ToolbarBtn}" Text="Save" Command="{Binding SaveProcessedAsyncCommand}"/>
                            <Button Style="{StaticResource ToolbarBtn}" Text="Load" Command="{Binding LoadLatestWorkspaceResultCommand}"/>
                            <Button Style="{StaticResource ToolbarBtn}" Text="Clear" TextColor="#f87171" Command="{Binding ClearHistoryCommand}"/>
                        </HorizontalStackLayout>

                        <HorizontalStackLayout Grid.Column="2" Spacing="8" VerticalOptions="Center">
                            <Label Text="Display" Style="{StaticResource PropLabel}" FontSize="12"/>
                            <Picker ItemsSource="{Binding ConductivityDisplayModes}" SelectedItem="{Binding SelectedConductivityDisplayMode}" FontSize="Micro" WidthRequest="140"/>
                        </HorizontalStackLayout>
                    </Grid>
                </Border>

                <Grid Grid.Row="1" BackgroundColor="#090b10">
                    <Border HorizontalOptions="Center" VerticalOptions="Center"
                            WidthRequest="750" HeightRequest="750"
                            Stroke="{StaticResource BorderColor}" StrokeThickness="1"
                            BackgroundColor="#13161C">
                        <Grid Padding="24" RowDefinitions="Auto,*" RowSpacing="12">
                            <VerticalStackLayout Spacing="4">
                                <Label Text="Mesh visualization" TextColor="{StaticResource TextPrimary}" FontAttributes="Bold" FontSize="16"/>
                                <Label Text="This view matches the meshing and reconstruction pages. Load a workspace or processed dataset to continue." TextColor="{StaticResource TextSecondary}" FontSize="12" LineBreakMode="WordWrap"/>
                            </VerticalStackLayout>

                            <Border Grid.Row="1" BackgroundColor="#0b0e12" Stroke="{StaticResource BorderColor}" StrokeShape="RoundRectangle 10" Padding="20" HorizontalOptions="Fill" VerticalOptions="Fill">
                                <VerticalStackLayout Spacing="12" HorizontalOptions="Center" VerticalOptions="Center">
                                    <Label Text="Visualization unavailable" TextColor="{StaticResource TextPrimary}" FontSize="18" FontAttributes="Bold" HorizontalTextAlignment="Center"/>
                                    <Label Text="Mesh rendering now follows the same viewer used on the meshing and reconstruction pages. Reload your dataset to inspect results there." TextColor="{StaticResource TextSecondary}" FontSize="13" HorizontalTextAlignment="Center" LineBreakMode="WordWrap" WidthRequest="520"/>
                                    <HorizontalStackLayout Spacing="8" HorizontalOptions="Center">
                                        <Button Text="Load Workspace" Command="{Binding LoadLatestWorkspaceResultCommand}" Style="{StaticResource ToolbarBtn}"/>
                                        <Button Text="Import File" Command="{Binding ImportSavedResultAsyncCommand}" Style="{StaticResource ToolbarBtn}"/>
                                    </HorizontalStackLayout>
                                </VerticalStackLayout>
                            </Border>
                        </Grid>
                    </Border>
                </Grid>
            </Grid>

            <!-- Right sidebar shows statistics without custom canvases -->
            <Border Grid.Column="2" BackgroundColor="{StaticResource PanelBackground}" Stroke="{StaticResource BorderColor}" StrokeThickness="0">
                <Grid RowDefinitions="Auto, *" Padding="16">
                    <VerticalStackLayout Grid.Row="0" Spacing="4">
                        <Label Text="Statistics" FontAttributes="Bold" FontSize="16" TextColor="{StaticResource TextPrimary}"/>
                        <Label Text="Real-time data analysis" TextColor="{StaticResource TextSecondary}" FontSize="12"/>
                    </VerticalStackLayout>

                    <ScrollView Grid.Row="1" Padding="0,10,0,0">
                        <VerticalStackLayout Spacing="12">
                            <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto" ColumnSpacing="8" RowSpacing="8">
                                <Border Style="{StaticResource ToolbarBtn}" StrokeThickness="0" BackgroundColor="#0f172a" Padding="12">
                                    <VerticalStackLayout>
                                        <Label Text="Min" TextColor="{StaticResource TextSecondary}" FontSize="10"/>
                                        <Label Text="{Binding StatMin}" TextColor="{StaticResource PrimaryAccent}" FontAttributes="Bold" FontSize="16"/>
                                    </VerticalStackLayout>
                                </Border>
                                <Border Grid.Column="1" Style="{StaticResource ToolbarBtn}" StrokeThickness="0" BackgroundColor="#0f172a" Padding="12">
                                    <VerticalStackLayout>
                                        <Label Text="Max" TextColor="{StaticResource TextSecondary}" FontSize="10"/>
                                        <Label Text="{Binding StatMax}" TextColor="#f87171" FontAttributes="Bold" FontSize="16"/>
                                    </VerticalStackLayout>
                                </Border>
                                <Border Grid.Row="1" Grid.ColumnSpan="2" Style="{StaticResource ToolbarBtn}" StrokeThickness="0" BackgroundColor="#0f172a" Padding="12">
                                    <VerticalStackLayout>
                                        <Label Text="Average" TextColor="{StaticResource TextSecondary}" FontSize="10"/>
                                        <Label Text="{Binding StatAvg}" TextColor="{StaticResource TextPrimary}" FontAttributes="Bold" FontSize="18"/>
                                    </VerticalStackLayout>
                                </Border>
                            </Grid>

                            <Border BackgroundColor="#0f172a" Stroke="{StaticResource BorderColor}" StrokeShape="RoundRectangle 8" Padding="12">
                                <VerticalStackLayout Spacing="8">
                                    <Label Text="History" TextColor="{StaticResource TextSecondary}" FontSize="12"/>
                                    <CollectionView ItemsSource="{Binding HistoryLog}" HeightRequest="140">
                                        <CollectionView.ItemTemplate>
                                            <DataTemplate>
                                                <Label Text="{Binding .}" TextColor="{StaticResource TextPrimary}" FontSize="12" LineBreakMode="WordWrap"/>
                                            </DataTemplate>
                                        </CollectionView.ItemTemplate>
                                    </CollectionView>
                                </VerticalStackLayout>
                            </Border>
                        </VerticalStackLayout>
                    </ScrollView>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</ContentPage>
